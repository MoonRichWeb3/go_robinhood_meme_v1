# Robinhood Meme 登录与部署说明

- 最新更新时间：2026-09-03 10:55 (UTC+8)
- 适用范围：生产服务器登录、Docker/SQLite 初始化、代码部署与更新
- 来源清单：
  - 本仓库 `Dockerfile`
  - 本仓库 `docker-compose.yml`
  - 本仓库 `.env`
  - 本仓库 `go.mod`
  - GitHub 仓库：[https://github.com/MoonRichWeb3/go_robinhood_meme_v1](https://github.com/MoonRichWeb3/go_robinhood_meme_v1)

## 1. 服务器信息

- 主机：`107.148.227.179`
- SSH 端口：`47140`
- SSH 用户：`root`
- 部署目录：`/opt/go/go_robinhood_meme_v1`
- 操作系统：Ubuntu 24.04 x86_64

登录命令：

```bash
ssh -p 47140 root@107.148.227.179
```

SSH 密码在终端提示后人工输入，不写入仓库、脚本或命令参数。当前密码已经在聊天中出现，部署稳定后应更换。

## 2. Docker 与 SQLite

服务器安装命令：

```bash
apt-get update
apt-get install -y ca-certificates docker.io docker-compose-v2 sqlite3
systemctl enable --now docker
docker --version
docker compose version
sqlite3 --version
```

当前已安装：

- Docker 29.1.3
- Docker Compose 2.40.3
- SQLite CLI 3.45.1
- Docker 服务已启动并设置为开机自启

SQLite 说明：

- 项目通过 Go 依赖 `modernc.org/sqlite` 直接读写 SQLite，不依赖容器内的 `sqlite3` 命令。
- 数据库文件在容器内为 `/data/robinhood_meme.sqlite3`。
- Docker Compose 使用命名卷 `robinhood-data` 持久化数据。
- SQLite 是文件型数据库，没有数据库账号和密码，不能初始化类似 MySQL/PostgreSQL 的账号密码。
- 数据访问依靠服务器 root 权限、Docker 卷权限和 SSH 权限控制。

查看数据库：

```bash
cd /opt/go/go_robinhood_meme_v1
VOLUME_NAME="$(docker volume ls \
  --filter label=com.docker.compose.project="$(basename "$PWD")" \
  --filter label=com.docker.compose.volume=robinhood-data \
  --format '{{.Name}}')"
DB_PATH="$(docker volume inspect -f '{{.Mountpoint}}' "$VOLUME_NAME")/robinhood_meme.sqlite3"
sqlite3 "$DB_PATH"
```

## 3. 代码仓库

- 仓库地址：`https://github.com/MoonRichWeb3/go_robinhood_meme_v1.git`
- 部署分支：`main`
- 服务器角色：只拉取、构建和运行，禁止提交与推送
- 环境配置：`.env` 随仓库版本管理并部署

本机开发完成后统一提交到 `main` 并推送：

```bash
git checkout main
git pull --ff-only origin main
git add <本次修改文件>
git commit -m "<提交说明>"
git push origin main
```

## 4. 首次部署

```bash
install -d -m 0755 /opt/go/go_robinhood_meme_v1
cd /opt/go/go_robinhood_meme_v1
git clone --branch main --single-branch \
  https://github.com/MoonRichWeb3/go_robinhood_meme_v1.git .
```

安装服务器禁止提交 Hook：

```bash
cat > .git/hooks/prepare-commit-msg <<'EOF'
#!/bin/sh
echo "禁止在部署服务器创建 Git 提交；请在开发机提交并推送后再拉取。" >&2
exit 1
EOF
chmod 0555 .git/hooks/prepare-commit-msg
git config --local pull.ff only
git config --local receive.denyCurrentBranch refuse
```

说明：该 Hook 是部署操作约束。root 用户仍有能力删除 Hook，因此服务器运维流程必须坚持“只更新、不开发、不提交”。

构建并启动：

```bash
docker compose config
docker compose up -d --build
docker compose ps
curl --fail http://127.0.0.1:8888/health
```

## 5. 日常更新

服务器只允许从远端 `main` 获取版本：

```bash
cd /opt/go/go_robinhood_meme_v1
git fetch --prune origin main
git checkout main
git reset --hard origin/main
docker compose up -d --build
docker image prune -f
docker compose ps
curl --fail http://127.0.0.1:8888/health
```

`git reset --hard origin/main` 会清除服务器工作区改动，使部署目录严格等于远端版本。禁止直接在服务器修改代码或 `.env`。

## 6. 运维命令

```bash
cd /opt/go/go_robinhood_meme_v1

# 查看服务
docker compose ps

# 查看最近日志
docker compose logs --tail=200 robinhood-meme
tail -n 200 logs/robinhood-meme.log

# 持续查看日志
docker compose logs -f robinhood-meme
tail -f logs/robinhood-meme.log

# 重启
docker compose restart robinhood-meme

# 停止但保留数据库卷
docker compose down

# 启动
docker compose up -d
```

禁止执行 `docker compose down -v`，该命令会删除 SQLite 数据卷。
