# 03 架构图（C4 与运行视图）

- 最新更新时间：2026-09-02 14:26 (UTC+8)
- 适用范围：v0 架构的规范视图；与 `01`/`02` 正文配套，图文冲突时以需求为准、图服从正文
- 图规范：C4（Context / Container / Component）+ 部署 + 时序 + 逻辑数据模型。语法为 Mermaid，可在 GitHub / Cursor 预览
- 编号：`图 n` 全局唯一，正文用「见图 n」引用

**读图约定**

| 符号 | 含义 |
| --- | --- |
| 实线箭头 | 运行时调用或数据主路径 |
| 虚线箭头 | 运营手工 / 弱依赖 / 非事实源 |
| 粗框 | 本仓库要交付的进程或模块 |
| 灰色/外部框 | 系统边界外（链、运营、网页） |

不在图里画自动交易、私钥、第五类盘口。

---

## 图 1 — 系统上下文（C4 Context）

本服务在谁中间、依赖谁、不依赖谁。

```mermaid
flowchart TB
  subgraph External["系统边界外"]
    Ops["运营人员"]
    Chain["Robinhood Chain RPC"]
    Web["Debot / GMGN 网页\n非事实源"]
  end

  subgraph System["go_robinhood_meme_v1"]
    Svc["监听记账服务\n扫链 + 只读查询"]
    DB[("SQLite 挂卷")]
  end

  Ops -->|"改聪明钱名单 sqlite3"| DB
  Ops -->|"只读 HTTP：新盘 / 流水 / 健康"| Svc
  Ops -->|"看中文 stdout 日志"| Svc
  Svc -->|"读块、收据、日志"| Chain
  Svc -->|"读写业务数据"| DB
  Web -.->|"仅可作钱包名称出处"| Ops
```

---

## 图 2 — 容器（C4 Container）

一个进程内的运行时容器：谁写库、谁只读。

```mermaid
flowchart TB
  subgraph Process["单一 Go 进程"]
    HTTP["go-zero HTTP\n只读"]
    Scan["扫块循环\n主写"]
    Flush["刷价节拍\n默认 3s"]
    Score["评分节拍"]
    Mem["内存：有效名单 + 脏单价"]
  end

  RPC["链 RPC"]
  SQLITE[("SQLite WAL\n单 writer")]
  STDOUT["stdout 中文日志"]

  HTTP -->|"SELECT"| SQLITE
  Scan -->|"eth_getBlockReceipts 或 logs"| RPC
  Scan --> Mem
  Scan -->|"INSERT/UPDATE 新盘、交易、水位"| SQLITE
  Flush -->|"UPDATE launch_index.price_usd"| SQLITE
  Score -->|"UPDATE smart_wallets.score"| SQLITE
  Scan --> STDOUT
  Flush --> STDOUT
```

写路径必须汇到 **一个写连接 / 写队列**。HTTP 不得与扫块抢写。

---

## 图 3 — 组件与分层（C4 Component）

依赖只允许向下。`domain` 不碰 RPC/SQL。

```mermaid
flowchart TB
  CMD["cmd 启动组装"]
  APP["app 编排一块 / 节拍"]
  HTTP["httpsvc 只读 API"]

  subgraph Domain["domain 纯业务"]
    L4["四类 Launch 类型"]
    FIFO["FIFO 盈亏"]
    SCR["评分"]
    FILL["Fill 成交腿"]
  end

  subgraph Adapters["适配器"]
    VEN["venue 四盘口 + v4fill"]
    CAT["contracts 合约目录"]
    CH["chain RPC 水位"]
    ST["store SQLite"]
    WL["wallets 名单快照"]
    PR["price 折 U 与脏集合"]
    LG["logx 中文日志"]
  end

  CMD --> APP
  CMD --> HTTP
  APP --> VEN
  APP --> Domain
  APP --> ST
  APP --> WL
  APP --> PR
  APP --> LG
  HTTP --> ST
  VEN --> CAT
  VEN --> Domain
  VEN --> CH
  ST --> Domain
  PR --> Domain
  WL --> ST
  CH -.->|"无盘口 if"| CH
```

包路径与依赖禁则见 `01` §3。

---

## 图 4 — 部署（Docker）

```mermaid
flowchart LR
  subgraph Host["主机"]
    VOL["./data 卷"]
    ENV["环境变量\nRH_RPC_URL 等"]
    CLI["sqlite3 改名单"]
  end

  subgraph Ctr["容器"]
    BIN["Go 二进制"]
    MIG["migrations/*.sql"]
    DBFILE["/data/robinhood_meme.sqlite3"]
  end

  NODE["自有 RPC\n生产"]
  PUB["公开 RPC\n需 UA、易 429\n仅开发"]

  ENV --> BIN
  VOL --> DBFILE
  BIN --> DBFILE
  MIG -->|"启动执行"| DBFILE
  CLI --> VOL
  BIN -->|"生产"| NODE
  BIN -.->|"开发"| PUB
```

镜像不含密钥、不含 SQLite 数据文件。健康检查：进程 + 水位落后 + `/health`。

---

## 图 5 — 扫一块（运行时序）

```mermaid
sequenceDiagram
  participant Loop as 扫块循环
  participant Chain as chain
  participant WL as wallets
  participant Route as venue/route
  participant Ven as 盘口适配器
  participant Dom as domain
  participant Store as store
  participant Log as logx
  participant Dirty as price脏集合

  Loop->>Chain: 拉块收据
  Chain-->>Loop: receipts
  Loop->>WL: 刷新有效名单若过期
  loop 每条协议 log
    Loop->>Route: address + topic0
    alt 未登记
      Route-->>Loop: 忽略
    else 创建
      Route->>Ven: DecodeCreate
      Ven->>Store: launch_* + launch_index
      Ven->>Log: 新盘
      opt 创建人在有效名单
        Ven->>Store: wallet_events 发盘
        Ven->>Log: 聪明钱-发盘
      end
    else 成交 Fill
      Route->>Ven: DecodeFill 或 v4fill
      Ven->>Dirty: 已入库则记最新成交 U
      opt 用户在有效名单
        Ven->>Dom: FIFO 若卖出
        Ven->>Store: wallet_events
        Ven->>Log: 聪明钱-买/卖
      end
    end
  end
  Loop->>Store: 更新 sync_state
```

---

## 图 6 — 名单热更新

```mermaid
sequenceDiagram
  participant Ops as 运营 sqlite3
  participant DB as smart_wallets
  participant Tick as 刷新节拍 ≤1s
  participant Mem as 内存有效集合
  participant Scan as 扫块匹配

  Ops->>DB: INSERT/UPDATE/DELETE 或改 status
  Tick->>DB: SELECT address WHERE status=active
  alt 成功
    Tick->>Mem: 替换集合
  else 失败
    Tick->>Tick: 打错误、保留上一份
  end
  Scan->>Mem: 只用当前集合
  Note over Scan: 不回补已处理块
```

---

## 图 7 — 展示单价刷盘

```mermaid
sequenceDiagram
  participant Fill as 成交处理
  participant Mem as token→最新U
  participant T as 定时器 3s
  participant DB as launch_index
  participant Log as 价格日志

  Fill->>Mem: 已入库且算出 U 则标脏
  T->>Mem: 取出脏集合
  alt 价未变
    T-->>T: 跳过写库
  else 有变化
    T->>DB: UPDATE price_usd 等
    T->>Log: 每个脏代币一行 价格
  end
```

未成交代币不进入脏集合。明细表不写价格。

---

## 图 8 — 只读查询

```mermaid
sequenceDiagram
  participant U as 运营
  participant HTTP as httpsvc
  participant DB as SQLite 只读连接

  U->>HTTP: GET 健康 / 新盘 / 流水
  HTTP->>DB: SELECT
  DB-->>HTTP: 行
  HTTP-->>U: JSON
  Note over HTTP: 无写、无鉴权、默认本机
```

---

## 图 9 — 逻辑数据模型

物理 DDL 与列级定稿在 `docs/功能模块/`；本图约束主写与关系。

```mermaid
erDiagram
  SMART_WALLETS ||--o{ WALLET_EVENTS : "地址命中才记账"
  LAUNCH_INDEX ||--|| LAUNCH_PONS : "同类代币一行"
  LAUNCH_INDEX ||--|| LAUNCH_O1_STOCK : "同类代币一行"
  LAUNCH_INDEX ||--|| LAUNCH_O1_CRYPTO : "同类代币一行"
  LAUNCH_INDEX ||--|| LAUNCH_LONG : "同类代币一行"
  LAUNCH_INDEX ||--o{ WALLET_EVENTS : "token"
  QUOTE_ASSETS ||--o{ LAUNCH_INDEX : "配对报价"
  SYNC_STATE ||--o| LAUNCH_INDEX : "同链水位"

  SMART_WALLETS {
    text address PK "小写"
    text status "active/paused/blacklisted"
    real score
  }
  WALLET_EVENTS {
    text id PK "block+tx+log 拼接"
    text wallet_address
    text token_address
    text exec_usd_per_token "可空禁0"
  }
  LAUNCH_INDEX {
    text token_address PK
    text category
    text price_usd "3s 刷 可空禁0"
  }
  SYNC_STATE {
    text name PK "rh_4663"
    int last_block
    text last_hash
  }
```

---

## 图 10 — 表主写归属

```mermaid
flowchart LR
  Ops["运营 sqlite3"] --> SW["smart_wallets\n名单行"]
  Scan["扫块"] --> L["launch_* + index 建行"]
  Scan --> E["wallet_events"]
  Scan --> SS["sync_state"]
  Scan --> SW2["smart_wallets.last_seen_at"]
  Flush["刷价"] --> P["launch_index.price_usd"]
  Score["评分"] --> SC["smart_wallets.score"]
```

禁止 HTTP 写任何表。禁止刷价写 `wallet_events`。

---

## 图 11 — 合约目录分流（一块内每条 log）

```mermaid
flowchart TD
  Log["log.address + topic0"] --> Cat{"合约目录命中?"}
  Cat -->|否| Drop["忽略 不猜盘口"]
  Cat -->|是| Role{"role / venue"}
  Role -->|ignore Airlock 等| Drop
  Role -->|四类工厂创建| VCreate["对应 Venue.DecodeCreate"]
  Role -->|Pons 曲线买卖毕业| VPons["venue/pons"]
  Role -->|PoolManager Swap| Pool{"poolId 已登记?"}
  Pool -->|否| Drop
  Pool -->|是| V4["venue/v4fill → Fill"]
  Role -->|Initialize 且本盘创建| Reg["写入 pool 登记"]
```

---

## 图 12 — 四类盘口与共享通道

```mermaid
flowchart TB
  subgraph Private["盘口私有 创建"]
    P["pons 工厂 / LaunchAndBuy"]
    OS["o1 股票工厂"]
    OC["o1 加密工厂"]
    L["LongLauncher 仅此"]
  end

  subgraph Shared["共享"]
    PM["PoolManager Swap"]
    REG["池登记 poolId→token+category"]
    CURVE["Pons 曲线地址→token"]
  end

  P --> IDX["launch_index + launch_pons"]
  OS --> IDX2["launch_index + launch_o1_stock"]
  OC --> IDX3["launch_index + launch_o1_crypto"]
  L --> IDX4["launch_index + launch_long"]
  P --> CURVE
  OS --> REG
  OC --> REG
  L --> REG
  P -.->|"毕业后"| REG
  PM --> REG
  CURVE -->|"CurveBuy/Sell"| Fill["Fill"]
  REG --> Fill
```

Airlock 不进入「创建」框。

---

## 图 13 — 失败与水位

```mermaid
flowchart TD
  A["处理 log"] --> B{"解码成功?"}
  B -->|否 已知 topic| E1["错误日志 跳过该 log"]
  B -->|是| C{"写库"}
  C -->|主键冲突| Skip["跳过 幂等"]
  C -->|新盘分类冲突| E2["错误 不覆盖"]
  C -->|成功| OK["提交"]
  E1 --> W["sync_state 仍前进"]
  Skip --> W
  E2 --> W
  OK --> W
  W --> R{"块 hash 分叉?"}
  R -->|是| L["日志 + 更新水位 hash\n不删已有行"]
  R -->|否| Next["下一块"]
```

---

## 图 14 — 质量属性如何落地

```mermaid
flowchart LR
  subgraph Q["质量目标"]
    Q1["跟上链"]
    Q2["名单 ≤1s"]
    Q3["单价 ≤3s"]
    Q4["幂等"]
    Q5["不丢数"]
    Q6["可测"]
  end

  subgraph M["机制"]
    M1["收据轮询"]
    M2["名单快照刷新"]
    M3["脏集合刷盘"]
    M4["自然键主键"]
    M5["SQLite 挂卷"]
    M6["domain 单测"]
  end

  Q1 --> M1
  Q2 --> M2
  Q3 --> M3
  Q4 --> M4
  Q5 --> M5
  Q6 --> M6
```
