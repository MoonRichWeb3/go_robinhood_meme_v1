# 00 阅读约定

- 最新更新时间：2026-09-02 14:58 (UTC+8)
- 适用范围：本目录每一篇功能模块文档的结构；写代码时按节核对，禁止跳过「失败」和「不做」
- 来源：`docs/架构/01` 分层；`docs/需求/01` 已拍板约束

## 每篇固定章节

| 节 | 实现时干什么 |
| --- | --- |
| 职责 | 这个包允许做什么 |
| 边界 | 明确不做什么，避免临时加功能 |
| 输入 / 输出 | 函数签名级别的数据，不是散文 |
| 主路径 | 编号步骤，对应 `load → classify → build → commit` |
| 落库 / 内存 | 写哪张表、哪块内存；主写是谁 |
| 日志 | 中文标签与必填键 |
| 失败 | 每条失败打什么、水位动不动、是否写库 |
| 依赖 | 只许调用哪些模块 |
| 验收 | 对照需求 AC 或本模块用例 |
| 调研 | 打开哪份链上文档，禁止把公式抄错地方 |

**文件名与目录**必须以 [`19-代码目录与文件清单.md`](19-代码目录与文件清单.md) 为准，禁止按 Fourmeme 另起 `internal/core/parser`。

## 本期已定（写代码不要再当 TODO）

| 项 | 口径 | 文档 |
| --- | --- | --- |
| ETH 美元 | 币安 HTTP `ETHUSDT`，独立协程，禁止写 0 | 调研 `10`、模块 `11` |
| 落地盘口 | 只 Pons + o1 加密 | 模块 `02`/`04`/`05`/`06` |
| 交易表 | 只留 7 天，小时分批删 | 模块 `18` |
| 收据 | 优先 `eth_getBlockReceipts`，不支持则进程内固定回退 | 模块 `01` |
| 信号第 3 类 | v0 用「高分 + 近 7 日有发盘」，不算分数斜率 | 模块 `15` |

股票美元、o1 股票、Long：**本期不做**。恢复时改模块 `04`/`06`/`11`，不要在本期代码里留 `TODO(feed)` / `TODO(score-hist)`。扫到股票 quote 仍缺价、禁止写 0。

## 禁止

- 一个文件里拉 RPC + 解四种 ABI + 写四张表 + 算 FIFO
- 把 Debot 名称当分类
- 把 `Swap.sender` 当用户钱包
- 交易表写全市场成交
- 明细表写价格、写 `raw_json`
- 自动买卖代码
- 另起 `internal/core/parser` / `internal/trade`（路径见模块 `19`）
